    //Validity: If a correct process broadcsts m, then every correct process eventually delivers m.

    //No duplication: Every corerct process delivers at most one message.

    //Integrity: If some correct process delivers m with sender p and p is correct, then m was previously broadcast by p..

    //Consistency: If some correct process delivers m and another correct process delivers m' then m==m'.

    //Totality: If some message is delivered by any correct process then every correct process eventually delivers some message.


    val agreement = processes.forall(x => 
        honest(x) implies or {
            context.accepted.get(x) == None,
            processes.forall(y => 
                honest(y) implies or {
                    context.accepted.get(y) == None,
                    context.accepted.get(x) == context.accepted.get(y)
                })
        })